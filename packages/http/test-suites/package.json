{
  "name": "@duck-decoy/http-test-suites",
  "version": "0.1.4",
  "private": true,
  "scripts": {
    "ci:test": "vitest run --reporter=dot --no-color",
    "lint": "pnpm exec eslint .",
    "test": "vitest run --reporter verbose",
    "test:e2e": "pnpm test"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@duck-decoy/fastify": "*",
    "@duck-decoy/koa": "*",
    "@duck-decoy/swagger": "*",
    "axios": "^1.11.0",
    "duck-decoy": "*",
    "ts-node": "^10.9.2",
    "vitest": "^3.2.4"
  }
}
