{
  "name": "@duck-decoy/http-test-suites",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "ci:test": "vitest run --reporter=dot --no-color",
    "lint": "pnpm exec eslint .",
    "test": "vitest run --reporter verbose",
    "test:e2e": "pnpm test"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@duck-decoy/fastify": "workspace:*",
    "@duck-decoy/koa": "workspace:*",
    "@duck-decoy/swagger": "workspace:*",
    "axios": "^1.11.0",
    "duck-decoy": "workspace:*",
    "ts-node": "^10.9.2",
    "vitest": "^3.2.4"
  }
}
